name: CI|CD

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - develop

jobs:
    Lint:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                python-version: [ 3.12, 3.10.12 ]
        steps:
            - name: Download repo
              uses: actions/checkout@v2
              with:
                python-version: ${{ matrix.python-version }}

            - name: Setup Python ${{ matrix.python-version }}
              uses: actions/setup-python@v2
              with:
                python-version: ${{ matrix.python-version }}

            - name: Upgrade pip
              run: |
                  python3 -m pip install --upgrade pip

            - name: Install dependencies
              run: |
                  pip3 install types-psutil flake8
                  pip3 install -r requirements.txt

            - name: Flake8
              run: |
                  flake8 --extend-ignore E501,F405

    Tests:
        needs: Lint
        runs-on: ubuntu-latest
        strategy:
            matrix:
                python-version: [ 3.12, 3.10.12 ]
        steps:
            - name: Download repo
              uses: actions/checkout@v2
              with:
                python-version: ${{ matrix.python-version }}

            - name: Setup Python ${{ matrix.python-version }}
              uses: actions/setup-python@v2
              with:
                python-version: ${{ matrix.python-version }}

            - name: Install dependencies
              run: |
                pip3 install -r requirements.txt
                pip3 install mypy types-psutil

            - name: MyPy
              run: |
                  mypy .

    Deploy:
      #needs: Tests
      runs-on: ubuntu-latest
      steps:
        - name: Connect and pull 0
          if: github.event_name == 'push' && github.ref == 'refs/heads/master'
          run: |
            mkdir -p $GITHUB_WORKSPACE/.ssh
        - name: Connect and pull 1
          if: github.event_name == 'push' && github.ref == 'refs/heads/master'
          run: |
            touch $GITHUB_WORKSPACE/.ssh/id_rsa
        - name: Connect and pull 2
          if: github.event_name == 'push' && github.ref == 'refs/heads/master'
          run: |
            chmod 600 $GITHUB_WORKSPACE/.ssh/id_rsa
        - name: Connect and pull 3
          if: github.event_name == 'push' && github.ref == 'refs/heads/master'
          run: |
            echo "${{ secrets.SSH_PRIVATE_KEY }}" >> $GITHUB_WORKSPACE/.ssh/id_rsa
        - name: Connect and pull 4
          if: github.event_name == 'push' && github.ref == 'refs/heads/master'
          run: |
            ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "cd ${{ secrets.WORK_DIR }} && git checkout ${{ secrets.MAIN_BRANCH }} && git pull && exit 0"

        - name: Cleanup
          run: rm -rf $GITHUB_WORKSPACE/.ssh && exit 0